# 仓库使用规则

## 目标
保持 `nanobot-private` 作为多节点运维脚本与模板的统一分发源。

## 规则
1. 不提交真实密钥、密码、token、明文 `.env`
2. 通用脚本放 `scripts/`，节点差异不要硬编码在通用脚本里
3. 模板放 `configs/templates/`，节点覆盖示例放 `configs/nodes/`
4. 机器说明与限制写入 `docs/nodes/`
5. 可复用操作流程写入 `docs/runbooks/`
6. 涉及多节点的统一约定，优先先写文档再写脚本
7. 仓库内脚本默认要求幂等：重复执行不应造成明显破坏
