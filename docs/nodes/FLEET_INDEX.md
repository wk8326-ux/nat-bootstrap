# 节点索引

## 已接入 Git 私有仓库的节点
- `vps` → `/opt/nanobot-private`
- `fox` → `/opt/nanobot-private`
- `aws` → `/opt/nanobot-private`
- `oracle1` → `/opt/nanobot-private`
- `oracle4` → `/opt/nanobot-private`

## 统一 Git 约定
- 仓库：`wk8326-ux/nanobot-private`
- SSH Host：`github-nanobot-private`
- 节点本地私钥路径：`/root/.ssh/nanobot_private_deploy`
- 节点更新命令：`cd /opt/nanobot-private && git fetch origin main && git reset --hard origin/main`

## 当前未接入
- `local`（Windows）
